#include "main.h"

/**
 * _islower - check the name
 * @c: the integer to be checked
 *
 * Return: Always 0
 */

int _islower(int c)
{
	if (c >= 97 && <= 122)
		_putchar('1' + '0');
	else
		_putchar('0' + '0');

}
