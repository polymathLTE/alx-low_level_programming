#include <stdio.h>

/**
 * main - entry point
 *
 * Description - this task involves creating a function that displays
 * the alphabet in lowercase
 *
 * Return: (0) success
 */

int main(void)
{
char ch;

for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);

return (0);
}
