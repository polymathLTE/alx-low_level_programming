#include "main.h"
#include <string.h>

/**
 * _strlen - function name
 * @s: char pointer
 *
 * Description: returns the length of a string
 *
 * Return: (size) computed value
 */

int _strlen(char *s)
{
	int size;

	size = strlen(s);

	return (size);
}
